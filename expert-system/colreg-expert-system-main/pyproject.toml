[project]
name = "expert-system"
version = "0.1.0"
description = "COLREGs-72 expert system for ship collision avoidance"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
    "amqtt>=0.10.1",
]
